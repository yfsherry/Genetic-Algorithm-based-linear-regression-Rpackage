# Choose Parents

choose_parents <- function(pop, models, obj_fun, fit_fun, sel_mech) {

  # Evaluate models with objective function (obj_fun)

  # Rank chromosomes with fitness function (fit_fun)

  # Select parents with selection mechanism (sel_mech)

  return(2)
}
